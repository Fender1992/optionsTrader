# Data providers
ALPHAVANTAGE_API_KEY=

# Broker (Tradier for options)
TRADIER_ENV=paper   # paper|live
TRADIER_ACCOUNT_ID=
TRADIER_ACCESS_TOKEN=

# App auth (single user)
APP_USERNAME=you@example.com
APP_PASSWORD_HASH= # bcrypt hash (generate with helper script)
TOTP_SECRET=       # base32 secret for 2FA

# Trading safety
EXECUTION_MODE=paper   # paper|live
KILL_SWITCH=false      # true to immediately block live orders
IP_ALLOWLIST=          # optional comma-separated IPs

# Capital for position sizing
CAPITAL_DOLLARS=10000

# Email settings (for alerts)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# SMS settings (optional - Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=